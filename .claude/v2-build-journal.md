# Email System v2 — Build Journal

Running log of every completed task. Subagents receive this verbatim in their prompts so they understand:
- What helpers already exist and their exact signatures
- What naming conventions have been established
- What deviations from the plan have been made and why
- What files have been touched recently

Newest tasks go at the **bottom**. Append-only.

---

## Conventions so far

- Track 3 files use `<?php` open tag + single-line `ABSPATH` guard: `if ( ! defined( 'ABSPATH' ) ) exit;`
- New classes use `HL_` prefix; files named `class-hl-<slug>.php`; CLI classes live in `includes/cli/`; services in `includes/services/`
- PHP 7.4 type hints ARE used on new methods (return types + scalar params). No `mixed` (8.0+ only).
- Option keys defined as class constants, not magic strings
- CLI test harness pattern: private `assert_true`/`assert_equals` helpers, `$pass`/`$fail` counters, `WP_CLI::halt(1)` on failure, groups dispatched by name via `--only=<group>`
- New shared helpers load via a `// Shared helpers` sub-block in `hl-core.php` above the `// Email system` block (sits at line 131-133 as of Task 1)

## Helpers already available

- `HL_Audit_Service::log( $action_type, $data = array() )` — now never throws, never blocks caller. Internally: try/catch wrap + `$wpdb->insert === false` check both route to `self::record_audit_failure()` which error_logs `[HL_AUDIT_FAIL]` + bumps daily counter `hl_audit_fail_count_YYYY-MM-DD` (autoload=no). The counter bump is itself wrapped in try/catch for hard-DB-outage safety.
- `HL_Audit_Service::get_last_event( $entity_id, $action_type ): ?array` — returns the most recent `hl_audit_log` row (as ARRAY_A with `actor_name` JOIN) matching entity + action, or `null`. Uses `ORDER BY created_at DESC LIMIT 1`. **Performance note:** no composite index on `(entity_id, action_type, created_at)` today — Task 6 schema revision should add `KEY entity_action_time` if Force Resend history becomes a hot path. Single-col `entity_id` index keeps today's usage fine (low row counts). **Caller note:** only works when the audit row has a non-null `entity_id` — the `email_token_alias_hit` audit doesn't pass one, so tests for it must query `hl_audit_log` directly by `action_type`.
- `HL_Email_Recipient_Resolver` now supports 3 new token behaviors (Track 3 Task 23):
  - `assigned_mentor` — new token. Resolves via `hl_team_membership` (`membership_type='mentor'`), scoped to cycle via `hl_team.cycle_id`. Requires `user_id` + `cycle_id` in context; audit-logs `email_resolver_missing_context` on missing context. Excludes self-mentor case (triggering user who is themselves their team's mentor) with `mentor_tm.enrollment_id <> user_tm.enrollment_id`.
  - `observed_teacher` — renamed from `cc_teacher`. Reads BOTH `observed_teacher_user_id` (new) and `cc_teacher_user_id` (legacy) context keys so existing callers keep working.
  - `cc_teacher` — legacy alias, routes to `resolve_observed_teacher()` + emits `email_token_alias_hit` audit on every call (A.6.11 deprecation telemetry, removal target: 90 days of zero hits). Only current caller is `class-hl-email-automation-service.php:413` for classroom visit emails — low volume, no flood risk near-term.
  - The resolver class docblock token list now reads `triggering_user, assigned_coach, assigned_mentor, school_director, observed_teacher (alias: cc_teacher), role:X, static:email`.
- `HL_Email_Condition_Evaluator` (`includes/services/class-hl-email-condition-evaluator.php`) — now routes `enrollment.roles` conditions through `HL_Roles`. The branch is added via an early-return `if` block just above the generic `switch ( $op )` inside `evaluate_single()`. If you need to route another polymorphic field (e.g. `team.roles`), follow the same pattern: guard on `$field === '<name>' && class_exists('HL_Roles')`, handle each op, early-return per case. Unsupported ops (`gt`/`lt`) return `false` explicitly.
- `HL_Roles` (`includes/services/class-hl-roles.php`) — static-only, 4 methods:
  - `HL_Roles::parse_stored( mixed $stored ): array` — format-agnostic reader. Accepts JSON array, CSV, array, or null/empty. JSON objects (`{...}`) rejected → `[]`. Non-string entries filtered. Lowercased + trimmed + deduped. Order of returned slugs is not guaranteed.
  - `HL_Roles::has_role( mixed $stored, string $role ): bool` — exact-match role check. Fixes the `LIKE '%leader%'` substring bug.
  - `HL_Roles::sanitize_roles( $roles ): string` — normalise to canonical CSV (sorted alphabetically). Drops entries containing `,`. Returns `''` on empty/invalid.
  - `HL_Roles::scrub_is_complete(): bool` — reads `HL_Roles::OPTION_SCRUB_DONE` (`'hl_roles_scrub_done'`). Callers gate `FIND_IN_SET` usage on this.
  - Constant: `HL_Roles::OPTION_SCRUB_DONE = 'hl_roles_scrub_done'`
- `HL_CLI_Email_V2_Test` (`includes/cli/class-hl-cli-email-v2-test.php`) — CLI test harness registered as `wp hl-core email-v2-test`. Groups: `roles` (filled), `schema`/`cron`/`drafts`/`resolver`/`deliverability`/`audit` (stubs — filled by later tasks). Support `--only=<group>` flag.

## Known deviations from plans

- **Task 1** placed the `HL_Roles` require in a new `// Shared helpers` sub-block above the `// Email system` block in `hl-core.php`, rather than literally "immediately before `class-hl-email-recipient-resolver.php`" as the plan text said. Phase B and Phase D reviewers both accepted this as a reasonable grouping for a generic (non-email) helper.
- **Task 1** added type hints (PHP 7.4 scalar + return type) and a class constant (`OPTION_SCRUB_DONE`) beyond the literal plan text, per Phase D Sr SWE feedback. The plan's verbatim PHP had no type hints and used a magic option string.
- **Task 1** added a `{` rejection branch to `parse_stored()` so JSON objects yield `[]` instead of being tokenised as CSV garbage. This is new behaviour not specified in the plan but matches the docblock contract and closes a latent quirk Phase D flagged.

---

## Task log

### 2026-04-11 — Track 3 Task 1: HL_Roles helper class
- Created `includes/services/class-hl-roles.php` (~117 lines): 4 static methods, 1 class constant, PHP 7.4 type hints, JSON + CSV format-agnostic parser with `{...}` rejection.
- Created `includes/cli/class-hl-cli-email-v2-test.php` (~155 lines): CLI harness + filled `test_roles()` with 12 assertions covering `has_role` both formats both polarities, `sanitize_roles` happy + poison, `parse_stored` null/empty/object/mixed-type edges, `scrub_is_complete` both states with option cleanup.
- Modified `hl-core.php`: added `// Shared helpers` sub-block + require of `class-hl-roles.php` (line 131-133), added require + register for the CLI test class.
- Quality gate passed: Phase B (2 reviewers PASS), Phase D (Sr SWE 7.2/10 PASS, WP Expert 9.4/10 PASS). Phase E applied 4 hardening fixes inline (type hints, const, docblock fix, JSON object rejection, expanded tests). Phase G: 0 user-visible error risk (no callers yet — Task 2 will be the first consumer).
- **NOT YET DEPLOYED.** Syntax will be verified on test server at the 4-task foundation checkpoint (after Tasks 1, 2, 5, 23).
- Commit: `c1b6e86`

### 2026-04-11 — Track 3 Task 2: Route condition evaluator through HL_Roles
- Modified `includes/services/class-hl-email-condition-evaluator.php` (+37 lines): inserted a role-aware branch in `evaluate_single()` between `resolve_field()` and the generic `switch ( $op )`. Branch activates on `$field === 'enrollment.roles'`, routes `eq`/`neq`/`in`/`not_in`/`is_null`/`not_null` through `HL_Roles::has_role()` / `parse_stored()`, rejects unsupported ops (`gt`/`lt`) with explicit `return false`.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+93 lines): replaced `test_resolver()` stub with 13 assertions covering JSON + CSV happy path, false-match regression for both formats, all 6 op boundaries, case-insensitive matching, and unsupported-op rejection.
- Quality gate: Phase B combined reviewer PASS, Phase D Sr SWE 8.5/10 PASS, WP Expert 9.4/10 PASS. Phase E added docblock note on `is_null`/`not_null` semantic divergence + 6 boundary assertions (`is_null` on PHP null, `neq` positive, `not_null` positive, unsupported op rejection, `in` zero-match, `neq` negative symmetry, case-insensitive match). Phase G: 0 user-visible error risk (workflows using valid role values — `teacher`, `mentor`, `coach`, `school_leader` — don't collide under strict matching).
- Deferred design concerns (NOT applied in Task 2, belong to a future refactor): hard-coded `'enrollment.roles'` string literal could be extensibility-scoped via `HL_Roles::is_role_field()` registry, `class_exists('HL_Roles')` guard is defensive-but-smelly (could be hard-required), request-scoped memoization on `parse_stored` for high-throughput evaluation paths. All flagged by Phase D Sr SWE; all out of scope for Task 2's plan text.
- **First consumer of `HL_Roles`** — proves the helper works via the CLI test flow. Still not deployed; syntax verified at the 4-task foundation checkpoint.
- Commit: `41d8907`

### 2026-04-11 — Track 3 Task 5: HL_Audit_Service::get_last_event + hardened log()
- Modified `includes/services/class-hl-audit-service.php` (+62/-16): wrapped `log()` body in `try { ... } catch ( \Throwable $e )`, added `$wpdb->insert === false` check, factored the failure path into a new private `record_audit_failure()` helper that itself catches any Throwable. Added public `get_last_event( $entity_id, $action_type ): ?array` with `LEFT JOIN` on `{$wpdb->users}` for `actor_name`. Fixed a pre-existing `cycle_id` column-alignment typo.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+39/-1): filled `test_audit()` with 3 assertions + `$wpdb->delete` cleanup of the test rows so we don't pollute the audit log.
- Quality gate: combined Phase B+D reviewer PASS. One real hole caught: `$wpdb->insert` returns `false` on SQL errors WITHOUT throwing, so the plan's verbatim `try/catch` alone would miss the most common failure mode. Fixed inline by adding the return-value check + extracting a shared `record_audit_failure` helper. Schema concern noted: `hl_audit_log` has single-col indexes on `entity_id` and `action_type` but no composite `(entity_id, action_type, created_at)` — fine for today's low row counts, flag for Task 6 schema revision if Force Resend history becomes hot. Phase G: 0 user-visible error risk (all changes are infrastructure hardening; happy-path behavior identical to pre-Task-5).
- Track 1 Task 14 (Force Resend history display) now has its required helper.
- Commit: `8cbaa3a`

### 2026-04-11 — Track 3 Task 23: assigned_mentor resolver + cc_teacher alias
- Modified `includes/services/class-hl-email-recipient-resolver.php` (+102/-5): added `assigned_mentor` token case, renamed `resolve_cc_teacher()` → `resolve_observed_teacher()` with dual context key support, added new `resolve_assigned_mentor()` with 3-step SQL (enrollment → team mentor → user). Kept `cc_teacher` as a legacy alias that emits `email_token_alias_hit` audit on every hit. Updated class docblock token list.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+65): appended `test_resolver` smoke test for `assigned_mentor` + `cc_teacher` alias audit verification. Uses `log_id` snapshot pattern for bounded cleanup (never wipes pre-existing deprecation telemetry) and avoids the `gmdate` vs server-TZ `CURRENT_TIMESTAMP` drift issue the combined reviewer caught.
- Quality gate: combined reviewer caught one BLOCKING bug (self-mentor edge case — a user who is themselves the team mentor would be returned as their own mentor via the self-join). Fixed inline by adding `AND mentor_tm.enrollment_id <> user_tm.enrollment_id` to the join. Two SHOULD-fix test hygiene issues (destructive cleanup, TZ-mismatched query window) also fixed inline via the log_id snapshot pattern. Weak smoke assertion (`is_array` passes on empty result) accepted as-is — strengthening it would require synthetic fixture data. Phase G: 0-1/10 user-visible error risk — `assigned_mentor` is a NEW token with no existing callers; `cc_teacher` alias's dual-key reading preserves existing caller semantics.
- Schema confirmations made during review: `hl_team_membership` has `(team_id, enrollment_id, membership_type ENUM('mentor','member'))` matching the plan's assumption. `hl_enrollment.status` enum is `('active','inactive')` — the `'warning'` literal is dead but harmless (kept for parity with existing cron queries per Pre-Flight note 6).
- **ALL FOUR TRACK 3 FOUNDATION PREREQUISITES (Tasks 1, 2, 5, 23) now committed.** Next step: deploy the branch to the test server and run `wp hl-core email-v2-test` to verify all four CLI test groups pass. If green, merge `feature/email-v2-track3-backend` to `main` and start Track 1 (which depends on these four). Remaining Track 3 tasks (3, 4, 6-22, 24-32) can run in parallel with Track 1 after the merge.
- Commit: `3190f63`
- **Post-foundation deploy checkpoint** (2026-04-11): branch deployed to test server via SCP tarball. `wp hl-core email-v2-test --only=roles` 12/12 PASS, `--only=resolver` 16/16 PASS, `--only=audit` 3/3 PASS. `wp hl-core smoke-test` showed 6 pre-existing `hl_user_profile/completion_pct` failures (all unrelated to Track 3 changes — `completion_pct` lives in frontend files this branch never touched). Merged to main as `2b120d7` via `--no-ff`. Pushed origin/main.

### 2026-04-11 — Track 3 Foundation Polish pass
Cold-read fresh-eye audit found zero blockers but 10 polish items worth fixing before adding more Track 3 work or starting Track 1. Executed in order per the polish brief. Re-merge to main required — polish commits are on top of `2b120d7`.

- **§1 Tasks 3+4 — gated FIND_IN_SET migration** (resolver). `resolve_school_director()` and `resolve_role()` now use `scrub_done()` gate: post-Rev-37 path = FIND_IN_SET; pre-scrub = LIKE narrow + `HL_Roles::has_role()` PHP post-filter. Unified post-filter runs in both branches as defense-in-depth. Comma-poison rejection added to `resolve_role()` with `email_resolver_rejected_role` audit trail. New private `HL_Email_Recipient_Resolver::scrub_done()` helper — single removal point for Task 32's `class_exists` cleanup. CLI test_resolver gains substring-regression assertion (skips if fixture data lacks a clean school_leader cycle) + poison rejection assertion with bounded log_id cleanup. **Full quality gate run** (Phase B: 2 parallel reviewers, Phase D: Sr SWE + WP expert). 1 must-fix from Phase B#2 (poison test audit cleanup), resolved inline via log_id snapshot. 1 helper extraction from Phase D Sr SWE, resolved inline. Phase D WP expert surfaced a spurious schema concern (FIND_IN_SET vs JSON) — countered by the scrub_is_complete() gate + unified post-filter, not a Task 3/4 issue. Commit: `f2359e5`.
- **§3 Atomic audit failure counter** (audit service). `record_audit_failure()` now uses `UPDATE wp_options SET option_value = CAST(option_value AS UNSIGNED) + 1 WHERE option_name = %s` followed by `add_option(...)` on zero-affected-rows. Closes a read-modify-write race where concurrent failure cascades would lose increments via the prior `get_option` + `update_option` pattern. `add_option` is atomic via the option_name UNIQUE key — two racers both seeing a missing row produce one insert + one ignored duplicate-key error, not a lost increment. Outer try/catch preserved.
- **§4 Rate-limited missing-context audit** (resolver). `resolve_assigned_mentor()` gates its `email_resolver_missing_context` audit call on a 5-minute transient `hl_resolver_missing_ctx_assigned_mentor`. A misconfigured cron workflow firing every few minutes now writes one row per 5 minutes instead of N rows per tick.
- **§5 Typo guard on evaluator** (condition evaluator). Added a `preg_match('/^enrollment\.roles?$/i', $field)` branch after the role-aware branch. Catches `enrollment.role` (singular), `Enrollment.Roles` (case), etc. Fail-closed (returns `false` — workflow doesn't silently match). Emits `email_condition_field_typo` audit row with per-field-sha1 transient rate limit (`hl_condition_typo_<sha1>`). CLI assertion in test_resolver proves `enrollment.Roles` fails closed and emits the audit row, with bounded log_id cleanup + transient cleanup.
- **§6 Strengthened assigned_mentor smoke test**. Rewrote to select a fixture where a team has BOTH a member AND a distinct mentor (self-join excludes self-mentor). Asserts `count === 1` AND the returned user_id matches the team's mentor enrollment's user_id (queried explicitly). Added second self-mentor exclusion assertion: find a lone-mentor team, resolve `assigned_mentor` with that mentor as context, assert empty result. Both assertions [SKIP] via `WP_CLI::log` when fixture data is unavailable — never fake passes.
- **§7 get_last_event zero-entity guard** (audit service). Early return `null` when `$entity_id <= 0`. Without this, `get_last_event(0, ...)` matched every audit row with a null or zero entity_id (including system-level events that never passed one). CLI assertion added.
- **§8 Stale token list fixes**. `README.md:75` and `class-hl-admin-emails.php:287` both listed `cc_teacher` as a canonical token and omitted `assigned_mentor`/`observed_teacher`. Both updated to: `triggering_user, assigned_coach, assigned_mentor, school_director, observed_teacher, role:X, static:email (legacy alias: cc_teacher)`.
- **§9 Deferrals routed to correct downstream sessions.**
  - Track 3 plan Task 6 header now documents adding composite index `KEY entity_action_time (entity_id, action_type, created_at)` on `hl_audit_log` during Rev 35 migration with idempotency guard.
  - Track 3 plan Task 32 now has explicit polish-era cleanup sub-items: drop `class_exists('HL_Roles')` / `class_exists('HL_Audit_Service')` defensive guards (single removal point via `HL_Email_Recipient_Resolver::scrub_done()`) + verify the 2026-07-10 cc_teacher reminder is still present in STATUS.md.
  - Track 1 plan already lists `assigned_mentor`, `observed_teacher` canonical, and `cc_teacher` alias-only acceptance in the token registry + recipient picker task (verified via grep on lines 144-146, 424, 440, 537-538). No additions required.
  - STATUS.md gains a new `## Scheduled reviews` section with a dated 2026-07-10 entry for the cc_teacher 90-day deprecation review.
  - New memory `project_cc_teacher_deprecation_2026_07.md` saved with the same reminder + removal steps + spec ref (A.6.11).
- **§9 items NOT applied inline** (per brief — these are scope-creep):
  - `rejected_role` audit rate limit — §4 only mentioned `missing_context`. Flagged in reviewer synthesis but left untouched.
  - DB error swallowing (`$wpdb->get_results` returning null treated as empty) — flagged by Sr SWE, not in brief.
  - Seeded-fixture for Task 3 regression test — the brief explicitly said "if no school_leader row exists, log [SKIP]".
- Polish commits: `f2359e5` (Tasks 3+4 gated migration) + `6cc01dd` (polish bundle §3-§9).
- **Post-polish deploy checkpoint** (2026-04-11): all polish commits deployed to test server via SCP tarball. `wp hl-core email-v2-test` reports **39 passed, 0 failed** across all groups (roles 12, resolver 23, audit 4 — up from 31 pre-polish). `--only=resolver` and `--only=audit` dispatch separately confirmed working. `wp hl-core smoke-test` shows the same 6 pre-existing `hl_user_profile/completion_pct` failures (unchanged from the pre-polish baseline — polish pass introduced zero regressions).
- Critical wins on the live server: the strengthened `assigned_mentor` smoke test (§6) fired without [SKIP] — test server seed data contains a team with both a member and a distinct mentor, so the `count===1` assertion ran with `expected 1, got 1` and the `user_id` match landed with `expected 1571, got 1571`. The self-mentor exclusion test also ran (not skipped), confirming `expected 0, got 0`. The typo guard (§5) fired: `enrollment.Roles (capital R typo) fails closed` AND `emits email_condition_field_typo audit row` — both on real audit writes with bounded log_id cleanup. Every assertion is LIVE-DB exercised, not fake-passing via [SKIP] shortcuts.
- **Next step (post round-1): Session A (Track 3 continuation, Tasks 6-22, 24-32) and Session B (Track 1 admin UX) cleared to dispatch. Session C (Track 2 builder) clear anytime.** Track 3 foundation is now complete with polish — no further foundation work blocking downstream sessions.

### 2026-04-11 — Track 3 Foundation Polish pass — ROUND 2 (sealed)
Two items flagged as out-of-scope during round 1, executed as a sealed polish round before any Session A/B/C work starts.

- **R2 §1 — Rate-limit `email_resolver_rejected_role` audit.** Applied the same per-value transient pattern used by round-1 §5 typo guard (but with a 16-char sha1 prefix for key length consistency — minor nit noted below). Key format: `hl_resolver_rejected_role_` + `substr(sha1($normalised_role), 0, 16)`. TTL `5 * MINUTE_IN_SECONDS`. Write-once via `get_transient` / `set_transient`. The `return array()` fail-closed control flow is unchanged — only the audit write is gated. A misconfigured workflow retrying the same poison value every cron tick now writes at most 1 row per 5 minutes instead of N per tick.
- **R2 §2 — DB error observability.** New private `HL_Email_Recipient_Resolver::log_db_error_if_any( string $method_label ): void` helper. Reads `$wpdb->last_error`, snapshots the first 200 chars, RESETS `$wpdb->last_error` to `''` unconditionally (load-bearing for downstream query attribution), then rate-limits via per-method sha1 transient (`hl_resolver_db_error_` + 16-char sha1). Emits `email_resolver_db_error` audit row with `reason => label . ': ' . snippet` and `entity_type => 'email_workflow'`. The helper lives in the `// Helpers` section of the class, right after `scrub_done()`. Added 9 physical call sites across 4 resolver methods:
  - `resolve_assigned_coach()` — 1 site, label `assigned_coach:lookup`
  - `resolve_school_director()` — 3 sites: `school_director:school_lookup` (after school_id query) + `school_director:director_lookup` (both post-scrub FIND_IN_SET branch AND pre-scrub LIKE branch — shared label since they're the same query path semantically)
  - `resolve_assigned_mentor()` — 3 sites: `assigned_mentor:enrollment_lookup`, `assigned_mentor:mentor_join`, `assigned_mentor:user_lookup`
  - `resolve_role()` — 2 sites: `role:user_query` in both scrub branches
  - Post-filter `foreach` loops and PHP-side typo/poison rejection branches are NOT instrumented (not DB calls).
  - Fail-closed contract preserved: DB error still returns `array()`, the helper only observes and audits.
- **Helper order invariant (verified by both Phase B and Phase D reviewers):** `snapshot last_error → reset last_error = '' → (class_exists + transient guards) → audit write → set_transient`. The reset runs BEFORE the audit write so even if `HL_Audit_Service` is missing or the rate limit fires, `$wpdb->last_error` is cleared.
- **CLI test coverage added to `test_resolver()`** (3 new assertion blocks):
  - **R2 §1 rate-limit test**: uses a distinct poison value (`teacher,mentor,coach`) from the existing round-1 poison test (`teacher,mentor`) so transient keys don't collide. Clears transient, snapshots `log_id`, calls `resolve()` twice back-to-back with the same poison, asserts exactly 1 new row (not 2). Cleans up via bounded `DELETE ... WHERE log_id > snapshot` and clears the transient.
  - **R2 §2 happy-path negative**: added INSIDE the `$mentor_fixture` branch right after the existing `user_id` match assertion. Runs a fresh `assigned_mentor` resolve on the known-good fixture and asserts 0 new `email_resolver_db_error` rows emit above snapshot. Skips cleanly with the rest of the mentor fixture block if no fixture data is available.
  - **R2 §2 forced-error positive**: uses `\Closure::bind` to invoke the private helper directly. Injects `$wpdb->last_error = 'SIMULATED_TEST_ERROR_DO_NOT_ALARM'`, binds a closure to `HL_Email_Recipient_Resolver::instance()` with class scope, invokes `log_db_error_if_any('role:user_query')`, then asserts (a) exactly 1 new `email_resolver_db_error` row with reason matching `%SIMULATED_TEST_ERROR_DO_NOT_ALARM%` AND (b) `$wpdb->last_error === ''` after the helper call. Cleans up bounded row + transient. The `Closure::bind` approach was explicitly authorized by the brief as the cleanest test technique (alternatives: test-only public wrapper is invasive, reflection is equivalent coupling with uglier syntax).
- **Quality gate**: Phase B combined reviewer (1 agent, both SQL/WP lenses) PASS with 1 non-blocking nit (25-line deliberation comment block in Assertion C). Phase D combined reviewer (Sr SWE 9/10 + WP Expert 10/10) PASS with the same nit + a sha1 prefix-length consistency nit (round-1 typo guard uses full `sha1($field)` vs round-2 uses `substr(sha1, 0, 16)`). Phase E: trimmed the dead commentary block to 6 lines inline. sha1 prefix consistency nit NOT addressed — changing round-1's key would orphan existing transients and is out-of-brief scope. **Phase G: 0/10 user-visible error risk** — this round adds pure observability (no return-value changes, no SQL changes, fail-closed preserved) and reuses the round-1 typo-guard rate-limit pattern which is already production-verified.
- **Items explicitly NOT applied** (per brief "sealed round" scope): no other resolver methods touched, no evaluator changes, no audit service changes, no plan file changes, no STATUS/README changes (foundation checkboxes already marked, no new feature surface).
- **Expected assertion count delta after R2**: round-1 finished at 39 (roles 12 / resolver 23 / audit 4). R2 adds 4 resolver assertions (1 rate-limit + 1 happy-path negative + 2 forced-error). Expected post-R2 total: **43** on a test server with the mentor fixture present (if mentor fixture absent, the happy-path negative skips and total is 42).
- Commit: `8413ebb`
- **Post-commit deploy checkpoint** (2026-04-11): deployed via SCP tarball. `wp hl-core email-v2-test --only=resolver` reported 27/27 PASS including all 4 new R2 assertions (rate-limit, happy-path zero errors, forced-error with SIMULATED marker, last_error reset). The full sweep (`wp hl-core email-v2-test` without --only) initially reported **42 passed / 1 failed** — the round-1 existing poison rejection test failed because my new R2 rate limit blocked its audit write on the second run within a 5-minute window. Root cause: round-1's poison test (`teacher,mentor`) didn't clear the rate-limit transient that R2 §1 now sets. Fix in commit `1e72ecb`: added `delete_transient($poison_lock)` before AND after the round-1 poison test so it's self-contained and idempotent across runs. Re-deployed, re-ran: full sweep **43/43 PASS**, `--only=resolver` 27/27, `--only=audit` 4/4, `--only=roles` 12/12. Smoke test: same 6 pre-existing `hl_user_profile/completion_pct` failures — no new regressions. Sealed at merge commit `c765046`.

### 2026-04-11 — Track 3 Task 6: Schema Rev 35 — hl_component window columns + composite indexes (+ entity_action_time polish addition)
- Modified `includes/class-hl-installer.php` (+153/-1): bumped `$current_revision` from 34 to 35; added Rev 35 ladder block that calls `migrate_component_add_window_cols()` and bails without bumping the revision on failure so retries are clean; added private static `migrate_component_add_window_cols(): bool` handling 5 ALTERs across 3 tables (hl_component: +available_from DATE, +available_to DATE, +INDEX type_pathway; hl_coaching_session: +INDEX component_mentor_status; hl_audit_log: +INDEX entity_action_time); all 5 ALTERs routed through a new private static helper `run_rev35_alter($sql, $label): bool` that logs `[HL_INSTALLER] Rev 35 migration failed on <label>: <$wpdb->last_error>` to error_log before returning false (Sr SWE observability fix — replaces 6 repeating branches with 1 helper + 5 call sites). Column adds guarded by `information_schema.COLUMNS` closure; index adds guarded by `information_schema.STATISTICS` closure; each target table guarded by `SHOW TABLES LIKE %s` closure. `CREATE TABLE hl_component` updated inline: `available_from`/`available_to` placed immediately after `ordering_hint` + new `KEY type_pathway (component_type, pathway_id)` in the keys block (coexists with the existing single-col `KEY component_type` — different query patterns, both intentional). `CREATE TABLE hl_audit_log` updated inline: `KEY entity_action_time (entity_id, action_type, created_at)` added between existing `KEY entity_id` and `KEY created_at`. `CREATE TABLE hl_coaching_session` updated inline: `KEY component_mentor_status (component_id, mentor_enrollment_id, session_status)` added at end of keys list. All 3 fresh-install bodies now match what the migration produces on existing installs.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+81/-2): filled `test_schema()` with 6 assertions — (1) `hl_component.available_from` column exists with `DATA_TYPE === 'date'`, (2) `hl_component.available_to` column exists with `DATA_TYPE === 'date'` (both use `information_schema.COLUMNS` + `DATA_TYPE` strictness from WP Expert review), (3) `hl_core_schema_revision >= 35`, (4) `hl_component.type_pathway` index exists, (5) `hl_audit_log.entity_action_time` index exists, (6) `hl_coaching_session.component_mentor_status` index exists. All use `$wpdb->prepare` with `information_schema.STATISTICS` / `information_schema.COLUMNS`, strict `===` comparisons against expected identifier names — no false-pass risk from truthy non-empty strings. Pure observation, no writes, no cleanup needed.
- **Full 8-phase quality gate run.** Phase A: fresh implementation subagent. Phase B: 2 parallel reviewers — R1 SQL/schema 9/10 PASS no blockers, R2 WP/idempotency 8/10 PASS_WITH_NITS flagged 3 should-fixes (CREATE TABLE inline parity for `component_mentor_status`, missing `error_log` on ALTER failure, missing test_schema assertion for `component_mentor_status`). Phase C: no Phase B disagreement to debate. Phase D: Sr SWE 7.9/10 APPROVE_WITH_FIXES agreed with all 3 R2 items + suggested the `run_rev35_alter` helper refactor; WP Expert 8.6/10 APPROVE_WITH_FIXES agreed with all 3 R2 items + added 1 real finding (assert `DATA_TYPE === 'date'` not just COLUMN_NAME existence — a future VARCHAR accident would have passed the weaker check). Phase E: all 4 fixes applied additively via a fresh implementation subagent — no Phase A logic reverted, helper refactor collapsed the 6 repeating `=== false` branches into 1 with centralized error_log, labels like `hl_component.available_from` / `hl_audit_log.entity_action_time` attached to each call site for log grep-ability. Phase F: fresh subagent cold-read flagged 7 strict-panel items (schema_last_error breadcrumb, SHOW TABLES LIKE wildcard escaping nit with standard `wp_` prefix, half-migration retry test, nullability assertion, SEQ_IN_INDEX ordering assertion, redundant single-col `entity_id` key, dbDelta reconciliation check) — none rise to must-fix because (a) nullable DATE columns with no backfill + additive indexes can't produce a user-visible error, (b) retry-on-next-load absorbs partial failures, (c) no shipping code reads the new columns or depends on the new indexes yet. Phase G: 0/10 error likelihood — user-facing surface is zero, happy path verified live on test server. Phase H: this entry + STATUS/README + commit.
- **A.2.19 verified:** `hl-core.php:268` hooks `plugins_loaded → HL_Core::init()`, `hl-core.php:292` calls `HL_Installer::maybe_upgrade()` unconditionally (not gated on activation), so git-deploy flows where `register_activation_hook` never fires still migrate on the next page load.
- **Live test server checkpoint:** deployed via SCP tarball. Forced `wp option update hl_core_schema_revision 34 && wp eval 'HL_Installer::maybe_upgrade();'` — revision bumped to 35 cleanly. `wp hl-core email-v2-test --only=schema` reports **6 passed / 0 failed**. Full sweep `wp hl-core email-v2-test` reports **49 passed / 0 failed** (43 foundation assertions still green from the round-2 sealed polish baseline + 6 new Rev 35 schema assertions). Zero regressions.
- Deferred strict-panel items (logged here for future polish passes, not applied in Task 6 scope):
  - Schema-last-error observability breadcrumb (transient or option) on top of error_log — useful if WP Admin UI wants to surface migration stalls.
  - `esc_like()` wrapping around `$table_name` in the `SHOW TABLES LIKE %s` closure — low risk with standard `wp_` prefix, but a strict WP-correct form.
  - Test assertion for SEQ_IN_INDEX column ordering on all 3 composite indexes — catches the "someone creates `type_pathway (pathway_id, component_type)` opposite order" regression.
  - `IS_NULLABLE = 'YES'` assertion on `available_from`/`available_to` — catches a future accidental `NOT NULL` change.
  - Consider dropping redundant single-col `KEY entity_id (entity_id)` on `hl_audit_log` in a future rev now that `entity_action_time (entity_id, ...)` covers the prefix.
- Commit: `42a8925`.

### 2026-04-11 — Track 3 Task 7: Admin pathway form — submission window date pickers
- Modified `includes/admin/class-hl-admin-pathways.php` (+65/-2): three surgical additions. (1) `render_component_form()` gets a new "Submission Window" `<tr>` inserted immediately after the Complete By row (line 1858) — two `<input type="date">` fields (`available_from` / `available_to`) side-by-side under a shared `<th>` label, each with `for`/`id` pairing + `aria-label` so screen-reader users hear "Submission window opens" / "Submission window closes" instead of two ambiguous "date input" announcements. Pre-fills from `$component->available_from` / `$component->available_to` on edit, empty on create. (2) `save_component()` gets a new submission-window persistence block placed AFTER the existing `scheduling_window_*`/`display_window_*` group (line 391) so all date-window fields live together. Validates via anchored `/^(\d{4})-(\d{2})-(\d{2})$/` regex + `checkdate((int)$m[2], (int)$m[3], (int)$m[1])` for actual calendar semantics — rejects `2026-02-31`, `2026-13-45`, etc. before MySQL strict mode would reject the INSERT. Silent swap on Opens > Closes via `strcmp` with a 60s per-user transient `hl_component_date_swap_<uid>`. Writes `$data['available_from']`/`$data['available_to']` which flow through the existing `$wpdb->insert`/`update` array form at lines 419/423 — verified no column whitelist in the save path, so no SQL modification needed. (3) New public method `maybe_render_date_swap_notice()` at line 1799 hooked to `admin_notices` in the constructor (line 36) — gated by (a) `current_user_can('manage_hl_core')` defense-in-depth matching the save-path gate, (b) `get_current_user_id()` non-zero check, (c) `get_current_screen()->id` contains `'hl-'` so the warning fires in context of the pathway admin, not on an unrelated Dashboard / Plugins / etc. page the admin might land on next. Notice delete-on-read prevents double-render.
- **No CLI assertions added** — Task 7 is a pure admin UI change. `test_drafts` / `test_cron` / etc. stubs in `class-hl-cli-email-v2-test.php` are untouched.
- **Full 8-phase quality gate run.** Phase A: fresh implementation subagent. Phase B: 2 parallel reviewers — R1 Admin UX 8/10 PASS_WITH_NITS (should-fixes: aria-label, screen-id gate for notice, optional component name in transient); R2 Security/escaping 9/10 PASS_WITH_NITS (should-fixes: `current_user_can` guard on notice, `is_string` pre-check on POST — the latter resolved by Phase D as already satisfied by the closure's own `is_string` guard). Nonce verified at line 206 (`hl_save_component`), capability gate at 207 (`manage_hl_core`). All echo paths `esc_html__` / `esc_attr`. No SQL injection surface (`$wpdb->insert` array form parameterizes values). Phase C: no Phase B disagreement. Phase D: combined senior review (simple admin form → collapsed from 2 agents to 1 per the quality-gate "simple task" exception) — 7.9/10 APPROVE_WITH_FIXES agreeing with R1a/R1b/R2a, dismissing R2b as already satisfied, adding TTL bump recommendation. Phase E: 4 fixes applied inline (aria-label, screen-id gate via `get_current_screen()->id` contains `'hl-'`, `current_user_can('manage_hl_core')` defense-in-depth guard, 30s→60s TTL). Phase F: cold-read senior review flagged regex-only date validation accepting `2026-02-31` (real bug — passes regex, MySQL strict mode rejects INSERT, save errors or partial-writes). Phase G initial: **2/10** with that failure mode named. Applied `checkdate()` fix inline — anchored regex now captures year/month/day and routes through `checkdate()` for calendar semantics. Phase G re-check: **1/10 READY_TO_COMMIT** — reviewer explicitly states "None material" for remaining modes; the 1/10 is scoring caution not a real concern.
- **Browser verification gap:** Per CLAUDE.md rule 6 (UI changes require browser test) and memory `feedback_test_before_claiming.md` (don't claim UI works from code reading alone), UI changes should be verified in a real browser before merge. This task was NOT browser-verified — admin login credentials are not available in this agentic run and the test server admin form cannot be loaded without them. Mitigation: (a) deployed to test server + full CLI sweep confirms zero PHP fatals and zero regressions (49/49 PASS); (b) explicit manual QA script documented in STATUS.md entry and this journal for the Track gate; (c) all quality-gate phases verified via code-read cold-review, so structural correctness is high confidence. **Recommend a batch manual UI pass at the Track gate covering Task 7 (date pickers + swap warning) alongside Task 8 (draft envelope UI) before merging feature/email-v2-track3-backend to main.**
- **Live test server checkpoint:** deployed via SCP tarball. Full sweep `wp hl-core email-v2-test` reports **49 passed / 0 failed** — unchanged from the post-Task-6 baseline. No new assertions (Task 7 is pure UI), no PHP fatals from the new admin form code. Zero regressions.
- Commit: `e7559be`.

### 2026-04-11 — Track 3 Task 8: Draft autosave envelope (created_at / updated_at)
- Modified `includes/admin/class-hl-admin-email-builder.php` (+56/-4): rewrote `ajax_autosave()` to wrap the draft payload in a JSON envelope `{created_at, updated_at, payload}` at option key `hl_email_draft_<user>_<template_id>`. First save stamps `created_at = gmdate('c')`; subsequent saves preserve it via `json_decode` of the existing option value. `updated_at` always refreshes to `$now_iso` so the Task 10 daily cleanup can compare against a sliding 30-day window. Draft-load path at line 61 rewritten to unwrap: if `json_decode` yields an array with a `payload` key, pass the inner string downstream; else fall through to legacy raw payload. Downstream contract preserved — `$draft_data` remains either `null` or a string, so line 80's truthy banner check and line 194's `wp_json_encode($draft_data)` that feeds the JS config work unchanged. Phase E added two defensive guards inline from the Phase B/D consensus: (1) empty-`$raw_payload` guard returns `wp_send_json_error('Empty draft payload')` so an accidental empty POST cannot overwrite a good prior draft; (2) `$envelope === false` guard catches `wp_json_encode` encode failures (invalid UTF-8, recursion, NaN/Inf) so the server never silently persists literal `false` and loses the user's in-flight draft. Concurrency note added as a comment — two overlapping first-ever autosaves could stamp slightly different `created_at` values in a microsecond race window, but the cleanup consumer is a 30-day staleness check, not an audit trail, so this is acceptable.
- A.6.6 defense: belt-and-suspenders `$wpdb->update($wpdb->options, ['autoload' => 'no'], ['option_name' => $key])` after every `update_option`, so legacy draft rows that were originally written with `autoload=yes` get flipped on the next save without needing a migration. Phase B noted the "extra query per autosave" overhead; accepted as intentional for Task 8 scope (Task 9 will add the bulk migration for the pre-existing legacy rows).
- **No CLI assertions added.** Task 8 is a pure admin UX change with no CLI-testable surface — the autosave path requires a nonce + capability + active admin session, which can't run inside the `wp hl-core email-v2-test` harness. `test_drafts()` stub remains stub for now; Task 10 cleanup may add coverage.
- **Full 8-phase quality gate run.** Phase A: fresh implementation subagent. Phase B: 1 combined reviewer (both server-side correctness and cross-layer JS contract preservation lenses) 9/10 PASS_WITH_NITS — 1 should-fix (wp_json_encode false check), 2 nits (extra wpdb->update per save; gmdate('c') vs Task 10 parser). Round-trip verified for all 4 cases: new / legacy raw payload / envelope second-save / corrupt envelope. JS contract verified via `email-builder.js:29-42` read — unwrapped payload string still JSON.parses correctly. Phase C: no disagreement (single reviewer). Phase D: combined senior (Sr SWE + WP Expert lenses) 8/10 APPROVE_WITH_FIXES — agreed with the encode-failure check, added empty-payload guard as a second defense-in-depth should-fix, flagged `gmdate('c')` format for Task 10's parser (not this task's scope). Phase E: 2 fixes applied inline (empty-payload guard + encode-failure guard), concurrency comment added. Phase F: cold-read subagent flagged size cap (no cap on `$raw_payload`), double-write overhead, `gmdate('c')` UTC vs `current_time('H:i:s')` response inconsistency — all theoretical / non-user-visible. Phase G: **1/10 READY_TO_COMMIT** — happy path solid, all flagged concerns are cosmetic or admin-only theoretical DoS behind `manage_hl_core`.
- **Live test server checkpoint:** deployed via SCP tarball. `wp hl-core email-v2-test` reports **49 passed / 0 failed** — unchanged (no new assertions). PHP syntax verified clean via the full sweep running without fatals.
- **Browser verification deferred:** same story as Task 7 — admin-only UI change, credentials not available in this agentic run. Manual QA script for the Track gate: (1) open the email builder, create a new template, wait 3s for autosave, verify `wp option get "hl_email_draft_1_new"` returns JSON with `created_at/updated_at/payload`; (2) refresh the page, verify the Restore banner appears and clicking Restore re-populates the blocks list; (3) edit the draft again, re-save, verify `created_at` is unchanged and `updated_at` advanced; (4) test legacy migration — `wp option update "hl_email_draft_<uid>_new" '{"blocks":[],"subject":"legacy"}' no`, then load the builder and verify the Restore banner still works (falls through to legacy branch).
- Commit: `ee2a4e2`.

### 2026-04-11 — Track 3 Task 9: Schema Rev 36 — autoload=no migration for existing drafts
- Modified `includes/class-hl-installer.php` (+53/-1): bumped `$current_revision` 35→36 at line 147, added Rev 36 ladder block below Rev 35 (bail-without-bump pattern mirrors Rev 35 exactly), added new private static `migrate_email_drafts_autoload_off(): bool` at the end of the class near `migrate_component_add_window_cols` / `run_rev35_alter`. The migration issues ONE prepared `UPDATE {$wpdb->options} SET autoload = 'no' WHERE option_name LIKE %s AND autoload != 'no'` with `$wpdb->esc_like('hl_email_draft_') . '%'` as the LIKE pattern. Idempotent: re-runs are 0-row UPDATEs. Uses `autoload != 'no'` rather than `= 'yes'` so the filter covers WP's legacy `'yes'` plus modern `'auto-on'` / `'auto-off'` / empty string values. On `$res === false`, logs `[HL_INSTALLER] Rev 36 migration failed: <last_error>` matching the Rev 35 convention and returns false so the ladder bails without bumping revision.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+63/-1): replaced `test_drafts()` stub with a 3-assertion test that seeds a `hl_email_draft_rev36_test_<random>` row via `wp_generate_password(12, false)` suffix + direct `$wpdb->insert` on `$wpdb->options` with `autoload='yes'`, invokes `HL_Installer::migrate_email_drafts_autoload_off()` via `Closure::bind($closure, null, HL_Installer::class)` (pattern borrowed from `test_resolver`'s `log_db_error_if_any` forced-error test), then asserts (1) migration returned true, (2) the seeded row's autoload column is now 'no', (3) `hl_core_schema_revision >= 36`. `wp_cache_delete` on the option key + `alloptions` before + after to keep the autoloader cache coherent. `delete_option` cleanup at the end so the test is self-contained across re-runs.
- **Task 9 collapsed the 8-phase gate into a single subagent** ("full gate inline") because the pattern mirrors Rev 35 exactly — the subagent ran Phase A implementation, self-reviewed through Phases B/C/D/F/G, and reported Phase G 0/10 with all strict-senior concerns accounted for (autoload enum coverage, concurrency, unindexed LIKE perf acceptable for one-time migration, test isolation). This is an application of the quality-gate's "small task" exception since Task 9 adds 1 prepared UPDATE + 3 CLI assertions + mirrors an already-reviewed pattern.
- **Live test server checkpoint:** deployed via SCP tarball. `wp option update hl_core_schema_revision 35 && wp eval 'HL_Installer::maybe_upgrade();' && wp option get hl_core_schema_revision` returned `36` cleanly. `wp hl-core email-v2-test --only=drafts` reports **3 passed / 0 failed**. Full sweep `wp hl-core email-v2-test` reports **52 passed / 0 failed** (49 post-Task-8 baseline + 3 new drafts assertions). Zero regressions.
- Commit: `0bc6bb0`.

### 2026-04-11 — Track 3 Task 10: cleanup_stale_drafts() daily sweeper
- Modified `includes/services/class-hl-email-automation-service.php` (+79/-3): new private instance method `cleanup_stale_drafts()` appended to `HL_Email_Automation_Service` before the closing class brace. Circuit-breaker: `$first_run = ! get_option('hl_email_draft_cleanup_seen')`, cap 5000 on first run, 500 steady-state. Threshold computed as `$threshold_ts = time() - 30 * DAY_IN_SECONDS` with `$cutoff = gmdate('c', $threshold_ts)` kept for the audit summary text. Selects `hl_email_draft_*` via `$wpdb->esc_like('hl_email_draft_') . '%'` + prepared `LIMIT %d`. Each row is `json_decode`d; corrupt (non-array) payloads increment `$skipped` and emit `HL_Audit_Service::log('email_draft_cleanup_skip', …)` with the offending `entity_id`, then continue. Valid envelopes read `updated_at` with a `created_at` fallback and a `'2000-01-01T00:00:00+00:00'` last-resort fallback; comparison uses `strtotime($updated_at) < $threshold_ts` so non-UTC offsets are handled correctly (Phase D consolidated reviewer flagged `strcmp` as lex-safe only against `+00:00` cutoffs — fixed inline by switching to numeric timestamp comparison). If `$to_delete` is non-empty, raw `DELETE FROM {$wpdb->options} WHERE option_id IN (<intval'd list>)` runs (safe by construction — int-cast + array_map + intval), immediately followed by `wp_cache_delete('alloptions', 'options')` so the autoloader cache can't resurrect deleted rows on the next request. Summary `HL_Audit_Service::log('email_draft_cleanup', ['reason' => 'deleted=X skipped=Y cap=Z'])` emits every run. `run_daily_checks()` TODO at lines 667-669 replaced with `$this->cleanup_stale_drafts();` wiring.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+98): APPENDED 3 new assertions to `test_drafts()` after the Task 9 Rev 36 block (original Task 9 assertions untouched). The new tests use a single `Closure::bind(function(){ $this->cleanup_stale_drafts(); }, HL_Email_Automation_Service::instance(), HL_Email_Automation_Service::class)` invoker — bound to the INSTANCE (not null) because `cleanup_stale_drafts` is a non-static private method. Envelopes are seeded via `wp_json_encode` → `update_option` to mirror Task 8's production write path (Phase A initially wrote them as PHP arrays and flagged the serialization mismatch in "reviewer double-check" — the test would have failed because the sweeper does `json_decode` on raw `$row->option_value` and PHP-serialized arrays decode to null → sweeper's corrupt branch). Fix applied pre-review. Three new assertions: (1) stale draft with `updated_at = now - 40d` deleted, (2) fresh draft with `updated_at = now` preserved (payload json-decoded + subject check), (3) direct-inserted raw string `'THIS IS NOT JSON'` preserved (corrupt-envelope skip path). Total drafts group assertion count: 6.
- **Full quality gate collapsed due to mature pattern:** Phase A fresh subagent implementation + self-review + consolidated Phase B+D reviewer (9/10 PASS, 1 non-blocking should-fix applied inline). Phase F/G collapsed into Phase B+D since the implementation mirrors the plan text closely. Phase G effectively 0/10 — user-facing surface is zero (cleanup runs in wp_cron daily tick, deletes oldstale drafts that are already abandoned, corrupt payloads explicitly protected).
- **Live test server checkpoint:** deployed via SCP tarball. `wp hl-core email-v2-test --only=drafts` 6/6 PASS. Full sweep `wp hl-core email-v2-test` reports **55 passed / 0 failed** (49 foundation + 6 drafts). Zero regressions.
- Commit: `b9fdd22`.

### 2026-04-11 — Track 3 Tasks 11 + 12 (batched): dedup drops date + column-exists guard
- Modified `includes/services/class-hl-email-automation-service.php` only. Bundled as one commit because both tasks are small, both modify `get_cron_trigger_users()` / `run_cron_workflow()` region, and separating them would produce merge churn.
- **Task 11 (A.1.6 / A.7.6):** Deleted `$date_bucket = gmdate('Y-m-d');` inside `run_cron_workflow()`. Replaced the `$dedup_token = md5(...)` input with a pipe-delimited `(trigger_key | workflow_id | user_id | entity_id | cycle_id)` tuple. Extended the existing class docblock (above the `class` keyword at line 30) with 3 contract paragraphs: (a) once-per-window dedup, (b) mid-window workflow behaviour, (c) timezone contract noting `current_time('Y-m-d')` + wp-cron ±24h edge drift. Missed cron days are now absorbed by the range predicate (same user still matches `available_from BETWEEN today AND today+7` the next day) instead of producing a distinct dedup token per day that duplicates reminders.
- **Task 12 (A.2.25):** New private static helper `has_component_window_column(): bool` placed between `get_cron_trigger_users()` and `cleanup_stale_drafts()`. Static `$cached` scoped inside the method so first call hits information_schema and subsequent calls skip the query. Top of `get_cron_trigger_users()` now short-circuits to `return array();` for the 4 window-dependent cron cases (`cv_window_7d`, `cv_overdue_1d`, `rp_window_7d`, `coaching_window_7d`) when `has_component_window_column()` returns false, with a request-scoped `static $warned` guard on the `error_log` so operators only see one line per request per missed-column deploy. `coaching_pre_end` is intentionally excluded from the list — it reads `hl_cycle.end_date`, not component window columns.
- **Full 8-phase quality gate collapsed to one subagent pass** — both tasks are mechanical plan-text transcriptions with clear scope, the subagent self-reviewed all phases and reported Phase G 0/10, cross-repo grep confirmed no stale `$date_bucket` references remain.
- **Live test server checkpoint:** deployed via SCP tarball. `wp hl-core email-v2-test` reports **55 passed / 0 failed** — unchanged (no new CLI assertions for this bundle; the dedup contract and column guard are both internal behaviours tested implicitly via Tasks 13-17's cron-query tests in a later batch). Zero regressions. Rev 35 is live on test server so `has_component_window_column()` returns true and the guard is a no-op there — exactly the expected happy path.
- Commit: `317a6ad`.

### 2026-04-11 — Track 3 Tasks 13-17 (batched): 5 real cron trigger queries
- Modified `includes/services/class-hl-email-automation-service.php` (+161/-8): replaced stub/combined case blocks in `get_cron_trigger_users()` with 5 live SQL queries. Pre-edit structure was 3 parts — `coaching_window_7d` and `coaching_pre_end` as individual stub `return array();` cases, and `cv_window_7d`/`cv_overdue_1d`/`rp_window_7d` as a combined fall-through block. Post-edit each of the 5 is a scoped `case 'X': { ... return $wpdb->get_results(...); }` block so per-case `$today`/`$plus7`/`$yesterday`/`$plus14` locals don't leak. Every query uses `$wpdb->prepare` with typed placeholders, `current_time('Y-m-d')` (site TZ per Task 11 contract), returns `ARRAY_A`, and carries a `LIMIT 5000` safety cap (Task 31 will add the warn-on-cap). Tasks 13+14+15 use `NOT EXISTS` subqueries (correct for the classroom_visit/rp_session lookup pattern where we need to dedup against a separate submission table). Tasks 16+17 use `LEFT JOIN ... WHERE cs.session_id IS NULL` anti-join (cheaper plan against the Rev 35 `component_mentor_status (component_id, mentor_enrollment_id, session_status)` composite index). Task 17 correctly keys on `hl_cycle.end_date BETWEEN today AND today+14` — that's why Task 12's column-exists guard intentionally excludes `coaching_pre_end` from its short-circuit list (it reads cycle end_date, not component window columns).
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+43): replaced `test_cron()` stub with a 5-assertion smoke test. Uses `ReflectionClass::getMethod('get_cron_trigger_users')` + `setAccessible(true)` to invoke the private method (no Closure::bind needed here because the reflection pattern reads more naturally for iterating multiple triggers). Fetches the first `hl_cycle` with `status='active'`, logs `[SKIP]` and returns if none (never fake-passes on empty fixture data). Iterates the 5 trigger keys, invokes each inside a try/catch that converts any `\Throwable` into a failed assertion with the exception message — guarantees the smoke test can't crash the test runner on a query fatal.
- **Full quality gate collapsed to one subagent pass** because all 5 queries are direct plan-text transcriptions, the subagent self-reviewed all phases (verified `$wpdb->prepare` placeholder typing, `LIMIT 5000`, `ARRAY_A`, `current_time` site-TZ, NOT EXISTS subquery outer-row scoping, plan SQL verbatim) and reported Phase G 0/10. Schema checks cross-referenced against `class-hl-installer.php` CREATE TABLE bodies for `hl_component`, `hl_pathway_assignment`, `hl_enrollment`, `hl_classroom_visit`, `hl_classroom_visit_submission`, `hl_rp_session`, `hl_rp_session_submission`, `hl_coaching_session`, `hl_cycle` — all column names match.
- **Live test server checkpoint:** deployed via SCP tarball. `wp hl-core email-v2-test --only=cron` reports **5 passed / 0 failed** on first run — all 5 queries execute without fatals against real schema. Full sweep `wp hl-core email-v2-test` reports **60 passed / 0 failed** (55 previous + 5 new cron). Zero regressions.
- Commit: `007a3dc`.

### 2026-04-11 — Track 3 Task 18: last_cron_run_at + Site Health staleness
- Modified `includes/services/class-hl-email-automation-service.php` only (+58): `run_daily_checks()` end now calls `update_option('hl_email_last_cron_run_at', gmdate('c'), false)` after the Task 10 `cleanup_stale_drafts()` call. New public static helper `cron_staleness_seconds(): int|null` reads the option + strtotime-parses it and returns the seconds-since-last-run (null if never set or unparseable). Constructor registers two new hooks: `site_status_tests` filter → `register_site_health_test()` which registers a `'direct'` bucket test `hl_email_cron_fresh`, and `admin_notices` → `maybe_render_cron_staleness_notice()` which screen-gates to `hl-emails*` via `get_current_screen()->id` strpos check. `site_health_cron_test()` returns the WP Site Health result array with `status='good'`/`'recommended'` based on a 36 * HOUR_IN_SECONDS threshold and an actionable description pointing at `wp cron event run hl_email_cron_daily`. Notice message uses `__()` + sprintf + esc_html ordering so translations can reposition the hour count correctly.
- **No CLI assertions added** — cron staleness is a time-dependent behaviour that would require clock mocking to exercise meaningfully in a unit test. Site Health result shape is effectively tested by WordPress core's Site Health screen itself.
- **Full 8-phase quality gate collapsed to one subagent pass** — plan text is mechanical, no SQL changes, no security surface beyond the existing admin-page capability gate (verified `class-hl-admin-emails.php:34` gates the whole page on `manage_hl_core`). Phase G 0/10. The subagent also caught and fixed one em-dash → `&mdash;` issue in the notice text inline to avoid mojibake risk on servers with fussy charset handling.
- **Live test server checkpoint:** deployed via SCP tarball. Full sweep `wp hl-core email-v2-test` reports **60 passed / 0 failed** — unchanged (no new assertions). Zero regressions.
- Commit: `c984da3`.

### 2026-04-11 — Track 3 Tasks 19-22 (batched): Rev 37 role scrub migration
- Created `includes/migrations/class-hl-roles-scrub-migration.php` — `HL_Roles_Scrub_Migration` class with `CHUNK_SIZE=500`, `FIRST_RUN_CHUNK=5000`, option keys `hl_roles_scrub_cursor`/`hl_roles_scrub_done`, 60s transient lock `hl_roles_scrub_lock`. `register()` hooks `maybe_run()` to `plugins_loaded@20`. `maybe_run()` guards on done flag + `is_admin() || WP_CLI` (never runs on visitor request path) + transient lock, wraps `run_chunk()` in try/catch with error_log on failure. `run_chunk()` reads cursor, sizes first chunk via backlog heuristic (`LIKE '[%' OR LIKE '% %'`), cursor-paginates `hl_enrollment` via `enrollment_id > %d ORDER BY enrollment_id ASC LIMIT %d`, wraps all UPDATEs in `START TRANSACTION`/`COMMIT`, ROLLs BACK on `$wpdb->update === false` + error_log + return-without-advancing-cursor. Idempotent short-circuit: `$normalized === (string) $row->roles` skips already-canonical rows without writing. Audit rows (`email_roles_scrub_chunk`/`email_roles_scrub_complete`) fire OUTSIDE the transaction boundary so a failing audit write never aborts the main transaction.
- Modified `hl-core.php` — added `require_once` for the new migration class in the `// Shared helpers` sub-block alongside `class-hl-roles.php`, and `HL_Roles_Scrub_Migration::register()` call in the bootstrap alongside other `::register()` calls.
- Modified `includes/class-hl-installer.php` — bumped `$current_revision` 36→37 at line 147, added Rev 37 ladder block below Rev 36 that bootstraps the `hl_roles_scrub_cursor`/`hl_roles_scrub_done` options to defaults (0/0) if they don't exist yet. The block sits BEFORE the `update_option('hl_core_schema_revision', ...)` call, so on a failure-retry path the options get re-initialized cleanly without clobbering a partial scrub's in-progress cursor.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` — added `[--run-scrub]` to the `## OPTIONS` docblock and a fast-path block at the top of `run()` that loops `HL_Roles_Scrub_Migration::run_chunk()` up to 100 times until `hl_roles_scrub_done=1`, with a `WP_CLI::halt(1)` if the safety counter expires.
- Modified `includes/domain/repositories/class-hl-enrollment-repository.php` — `create()` and `update()` both route `$data['roles']` through `class_exists('HL_Roles') ? HL_Roles::sanitize_roles(...) : (is_array(...) ? HL_DB_Utils::json_encode(...) : ...)` so post-scrub writes are CSV with a legacy fallback for partial-deploy safety.
- Modified 4 other `hl_enrollment.roles` write sites to use the same sanitize_roles wrapper: `includes/services/class-hl-import-participant-handler.php` (2 sites — CREATE branch at line 598, UPDATE branch at line 622), `includes/admin/class-hl-admin-enrollments.php:211` (admin manual enrollment save — uses direct `$wpdb->update` not the repo), `includes/cli/scripts/provision-test-teachers.php:64` (test seeder). Intentionally NOT touched (still JSON): `hl_pathway.target_roles` (tour repo + pathway repo + admin pathways), `hl_component.eligible_roles` (admin pathways:397) — these columns are still JSON per v1 behaviour and are explicitly preserved.
- **Live test server checkpoint:** deployed via SCP tarball. Rolled schema back (`wp option update hl_core_schema_revision 36`) and forced `HL_Installer::maybe_upgrade()` — option bumped to 37 cleanly, cursor initialized. `wp hl-core email-v2-test --run-scrub` reports "Scrub complete." in one CLI invocation (test server has small enrollment count, first chunk covers everything). Spot check: `SELECT enrollment_id, roles FROM wp_hl_enrollment LIMIT 10` returns 10 rows with `roles = "teacher"` (plain CSV, no brackets). `SELECT COUNT(*) FROM wp_hl_enrollment WHERE roles LIKE '[%'` returns **0** — all JSON converted. Full sweep `wp hl-core email-v2-test` reports **60 passed / 0 failed** unchanged. Zero regressions.
- **Full quality gate collapsed to one subagent pass** — the 4 tasks are tightly coupled (class → worker → repo writes → installer bump) and must land atomically. The subagent self-reviewed all phases and reported Phase G 0/10. No inline fixes needed — the plan text was directly transcribable and the subagent caught one natural deviation (explicit `$roles = array($role)` build line in the import handler to make the single-role wrapping path read cleanly).
- Commit: `bde5f3b`.

### 2026-04-11 — Track 3 Tasks 24-28 (batched): queue processor deliverability + reliability hardening
- Modified `includes/services/class-hl-email-queue-processor.php` (+92/-2): five tightly-linked changes across the class — subject encoding, deliverability headers, HMAC unsubscribe, wp_mail_failed capture, dynamic stuck threshold.
- **Task 24** (`process_single` line ~210-215): wraps subject in `mb_encode_mimeheader($subject, 'UTF-8', 'B')` with `function_exists` fallback for environments without mbstring. PHP's output keeps ASCII prefixes unencoded (`Bienvenida =?UTF-8?B?4oCU...?=`) — the test assertion caught this after a Phase A typo (initial `strpos === 0` expected the marker at position 0, corrected inline to `!== false` since the marker appears mid-string).
- **Task 25** (new `build_headers()` method at line ~352): constructs From/Reply-To/List-Unsubscribe headers from `hl_email_from_name` / `hl_email_from_email` / `hl_email_reply_to` options (with `get_bloginfo('name')` + `admin_email` defaults). `process_single` calls it instead of the old hardcoded `array('Content-Type: text/html; charset=UTF-8')`. The List-Unsubscribe pair (mailto + HTTPS) only emits when `build_unsubscribe_url()` returns non-empty — skipped for static-email recipients with no `recipient_user_id`.
- **Task 26** (new `unsubscribe_secret()` + `build_unsubscribe_url()` at lines ~373-399 + `HL_Installer::activate()` bootstrap lines 28-32): HMAC-signed one-click unsubscribe URLs via `hash_hmac('sha256', user_id . ':' . queue_id, $secret)`. Secret bootstrapped via `add_option('hl_email_unsubscribe_secret', wp_generate_password(64, true, true), '', 'no')` — `add_option` is atomic insert-if-missing so concurrent first-time callers can't double-generate. The runtime `unsubscribe_secret()` fallback covers git-deploy installs where `activate()` never fires. Returns `''` for rows with empty `recipient_user_id` so static-email fan-out doesn't get broken links.
- **Task 27** (new private `$current_row` property line ~29, `wp_mail_failed` hook registration line ~40, wrap in `process_single` lines ~214-216, handler at ~405-432): WP `wp_mail()` can return `true` but silently fail later via the `wp_mail_failed` action if SMTP rejects. The queue processor now sets `$this->current_row = $row;` immediately before `wp_mail` and nulls it immediately after. If `wp_mail_failed` fires during that window, `handle_wp_mail_failed()` flips the row to `status='failed'` with `failed_reason = 'wp_mail_failed: <200-char-excerpt>'` and emits an `email_wp_mail_failed` audit row.
- **Task 28** (new `stuck_threshold_seconds()` helper at line ~46, `recover_stuck_rows` line 337 updated): reads `wp_get_schedules()` for `five_minutes` or `every_minute` entries and returns `max(10 * $interval, 900)` so the stuck-row threshold scales with the processor's actual cron cadence instead of being hardcoded at 10 minutes. Falls through to `max(10*60, 900) = 900` default if no matching schedule is registered — safe. The old `STUCK_THRESHOLD_MINUTES = 10` const is intentionally left in place as dead code for a future cleanup commit to avoid scope-creep here.
- Modified `includes/class-hl-installer.php` (+5): `activate()` gains the `hl_email_unsubscribe_secret` bootstrap block — `add_option` atomic insert-if-missing with `wp_generate_password(64, true, true)` (64 chars, special chars allowed, extra special subset).
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+27): filled `test_deliverability` stub with 3 assertions covering the three load-bearing behaviours. Reflection-invoked `unsubscribe_secret()` to bootstrap the secret in a test context where `activate()` has already run.
- **Full quality gate collapsed to one subagent pass + one inline Phase E fix** — the Phase A subagent self-reviewed all phases and reported Phase G 0/10 but the live test caught a test-only bug where PHP's actual `mb_encode_mimeheader` output doesn't place the marker at position 0 (it keeps ASCII prefixes unencoded). 1-line assertion fix applied inline, re-deployed, full sweep 63/63 PASS.
- **Live test server checkpoint:** deployed via SCP tarball. `wp hl-core email-v2-test --only=deliverability` 3/3 PASS after the assertion fix. Full sweep `wp hl-core email-v2-test` reports **63 passed / 0 failed** (60 previous + 3 new deliverability). Zero regressions. PHP confirmed `mb_encode_mimeheader('Bienvenida — hoy comenzamos', 'UTF-8', 'B')` returns `Bienvenida =?UTF-8?B?4oCUIGhveSBjb21lbnphbW9z?=` — ASCII prefix preserved, only the non-ASCII portion encoded.
- Commit: `d5fb2dd`.

### 2026-04-11 — Track 3 Tasks 29-32 (batched) + post-scrub read-path regression fix — TRACK 3 COMPLETE
- **Task 29 (A.2.26):** 2 SELECT sites in `includes/admin/class-hl-admin-emails.php` got the `status != 'deleted'` filter — the unfiltered workflow list at line ~118 and the single-row load in `render_workflow_form()` at line ~175 (prevents direct-URL access to a soft-deleted workflow's edit screen). The filtered-list query is already safe (`$valid_statuses` excludes `deleted`) and automation service queries use `status = 'active'` which implicitly excludes `deleted` — no changes needed there.
- **Task 30:** Zero code changes. Verified no stray Force Resend references exist in `class-hl-admin-emails.php`. Force Resend is owned by Track 1 Task 14.
- **Task 31 (A.2.21):** Wrapped the `coaching_pre_end` case in `get_cron_trigger_users()` with a capture → 5000-cap check → audit → return pattern. SQL preserved byte-for-byte from Task 17; the only changes are capturing the result into `$rows`, checking `count($rows) >= 5000`, emitting `email_cron_safety_cap_hit` audit, and returning `is_array($rows) ? $rows : array()`.
- **Task 32 polish cleanup:** Dropped the `class_exists('HL_Roles')` guard from `HL_Email_Condition_Evaluator::evaluate_single()` roles-branch (line ~85) and from `HL_Email_Recipient_Resolver::scrub_done()` (the single extraction point added during foundation polish §1). `HL_Audit_Service` guards intentionally preserved. Verified `STATUS.md:257` still has the 2026-07-10 `cc_teacher` deprecation reminder under `## Scheduled reviews` (no action needed).
- **Task 32 regression sweep caught a real bug — FIXED inline:** `wp hl-core smoke-test` reported **6 new failures** (3 unique × 2 user contexts) in `hl_my_cycle`, `hl_team_progress`, and `hl_user_profile` with `in_array(): Argument #2 must be of type array, null given`. Root cause: Task 21's Enrollment Repository write path + Tasks 19-22's Rev 37 scrub converted `hl_enrollment.roles` from JSON array format to canonical CSV, but the plan's Task 21 pre-flight enumerated WRITE sites only — it missed READ sites that still called `json_decode($enrollment->roles, true)`, which returns null on a CSV string, cascading into `in_array($role, null)` fatals. `HL_Enrollment::get_roles_array()` was the domain model that both `hl_frontend_my_cycle:136` and `hl_frontend_user_profile:817` called through. Dispatched a comprehensive fix subagent that routed **25 files** through `HL_Roles::parse_stored()` (the format-agnostic foundation helper Task 1 landed for exactly this case): `includes/domain/class-hl-enrollment.php` + 10 admin files + 11 frontend files + 6 services + 2 integrations + 3 CLI files + auth service. The subagent also caught and fixed **latent title-case bugs** in 4 files (`class-hl-admin-classrooms.php`, `class-hl-admin-coaching.php`, `class-hl-frontend-cycle-dashboard.php`, `class-hl-observation-service.php`) where `in_array('Teacher', $roles)` / `in_array('Mentor', $roles)` checks would have silently returned empty against lowercase stored values regardless of format. These bugs predate Task 21 but became visible during the sweep.
- **Skipped write sites (not touched):** `hl_pathway.target_roles` (still JSON per v1), `hl_component.eligible_roles` (still JSON), `hl_product_tour.target_roles` (still JSON), `WP_User->roles` (WordPress core, always array), ELCPB CSV import reading `$row['roles']` from CSV input, automation-service.php passing `$enrollment->roles` as string context, admin-email-builder.php likewise.
- **Post-fix smoke test results:** `wp hl-core smoke-test` now reports **2 failures, both pre-existing `hl_user_profile/completion_pct` baseline** (Unknown column 'completion_pct' — a schema gap unrelated to Email System v2). The 3 new regression failures are eliminated. 2 ≤ 6 baseline ceiling — under budget.
- **Track gate green:** `wp hl-core email-v2-test` 63/63 PASS, `wp hl-core smoke-test` at or below baseline.
- **Known deferred item:** one remaining `class_exists('HL_Roles')` guard at `class-hl-email-recipient-resolver.php:501` inside `resolve_role()`'s post-filter loop — the task 32 scope was explicitly limited to the condition-evaluator + scrub_done() sites, so this is logged as a follow-up polish item but not applied in this commit.
- Commit: pending (this entry → git add → commit).

---

## Track 3 complete — session A exit summary

All 32 Track 3 tasks committed on `feature/email-v2-track3-backend`:
- Foundation (Tasks 1, 2, 3, 4, 5, 23): sealed in `c765046` pre-session
- Session A work (Tasks 6-22, 24-32): commits `42a8925` → `<pending final commit>`
- **Sweep: 63/63 PASS**, **smoke test: 2 failures (both baseline)**, zero new regressions after the read-path fix
- Ready for merge PR handoff to user — **do not self-merge** per execution rules

---

## Track 1 — Session B: Admin Workflow UX

### 2026-04-11 — Track 1 Tasks 1+2: Test harness + static registries
- Created `bin/test-email-v2-track1.php` (16 assertions): registries, generate_copy_name, operator_label, validate_workflow_payload. Uses `$GLOBALS` counters to work around `wp eval-file` scope issue. Manually loads `HL_Admin_Emails` class (not loaded in CLI context).
- Added 8 static methods to `HL_Admin_Emails`: `get_condition_fields()`, `get_condition_operators()`, `get_all_operator_labels()`, `operator_label()`, `get_recipient_tokens()`, `generate_copy_name()` (with table allowlist per code review), `validate_workflow_payload()`, `sanitize_json_payload()`.
- Code review caught and fixed: `generate_copy_name()` used `preg_replace` on table name — replaced with explicit allowlist (`hl_email_workflow`, `hl_email_template`).
- Commit: `35da1a6`.

### 2026-04-11 — Track 1 Task 4: cycle_id backfill in hydrate_context
- Modified `includes/services/class-hl-email-automation-service.php`: backfill `cycle_id` from `enrollment_id` at end of `hydrate_context()` for triggers whose sub-loaders don't set it.
- Commit: `44bd2cb`.

### 2026-04-11 — Track 1 Task 5: Harden handle_workflow_save
- Replaced `handle_workflow_save()` body: decodes/validates/re-encodes JSON via `sanitize_json_payload()` + `validate_workflow_payload()`. Stable `wp_json_encode` flags (A.3.5). Audit logging added.
- Commit: `2548583`.

### 2026-04-11 — Track 1 Task 6: Enqueue + registries + nonces
- Created `assets/js/admin/email-workflow.js` (stub — body class signal + error listener).
- Modified `class-hl-admin.php`: enqueue on workflow edit/new pages only, inline registries via `wp_add_inline_script('before')`, `wp_refresh_nonces` filter (A.2.24), eager `HL_Admin_Emails::instance()` for admin-post hooks.
- Commit: `b9b669f`.

### 2026-04-11 — Track 1 Tasks 7-10: Condition Builder + Recipient Picker
- Replaced JSON textareas in `render_workflow_form()` with visual builder shells + `<details>` fallback.
- Full JS implementation: condition row builder (field/op/value adaptation, pills, ARIA), recipient token cards with check/dim/disabled states, trigger-dependent visibility, Primary→CC exclusion, debounced AJAX recipient count.
- Added `ajax_recipient_count()` AJAX handler with `HL_Roles`-based role counting.
- Registered all admin-post hooks + stubs for Tasks 11/12/14.
- Commit: `40387a9`.

### 2026-04-11 — Track 1 Task 11: Workflow row actions
- Implemented `handle_workflow_duplicate()`, `handle_workflow_delete()` (soft-delete with queue guard + transaction), `ajax_workflow_toggle_status()`.
- Updated `render_workflows_tab()`: `.hl-email-admin` wrapper, full row actions markup, inline JS for confirm-delete + AJAX toggle.
- Added A.2.26 comments on automation-service SELECT queries.
- Commit: `d76d8ee`.

### 2026-04-11 — Track 1 Task 13: CSS polish
- Appended 201 lines to `assets/css/admin.css`: condition builder, pill tags, token cards, row actions, JS-fallback visibility. All scoped `.hl-email-admin`.
- Commit: `08d8ccc`.

### 2026-04-11 — Track 1 Tasks 12+14: Template actions + Force Resend
- Implemented `handle_template_duplicate()` (unique template_key generation), `handle_template_archive()`, `handle_workflow_force_resend()`.
- Updated `render_templates_tab()`: row actions markup + `.hl-email-admin` wrapper.
- Updated `render_workflow_form()`: template dropdown excludes archived (keeps assigned with suffix), Force Resend box with scope selector + audit history.
- Commit: `ad66cca`.

### Track 1 gate results
- Track 1 tests (`bin/test-email-v2-track1.php`): **16/16 PASS**
- Track 3 tests (`wp hl-core email-v2-test`): **63/63 PASS** (zero regressions)
- Smoke test (`wp hl-core smoke-test`): **2 failures** (both pre-existing `hl_user_profile/completion_pct` baseline)
- Browser verification: **pending manual UI pass at PR review time**


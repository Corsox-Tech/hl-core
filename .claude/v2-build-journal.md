# Email System v2 — Build Journal

Running log of every completed task. Subagents receive this verbatim in their prompts so they understand:
- What helpers already exist and their exact signatures
- What naming conventions have been established
- What deviations from the plan have been made and why
- What files have been touched recently

Newest tasks go at the **bottom**. Append-only.

---

## Conventions so far

- Track 3 files use `<?php` open tag + single-line `ABSPATH` guard: `if ( ! defined( 'ABSPATH' ) ) exit;`
- New classes use `HL_` prefix; files named `class-hl-<slug>.php`; CLI classes live in `includes/cli/`; services in `includes/services/`
- PHP 7.4 type hints ARE used on new methods (return types + scalar params). No `mixed` (8.0+ only).
- Option keys defined as class constants, not magic strings
- CLI test harness pattern: private `assert_true`/`assert_equals` helpers, `$pass`/`$fail` counters, `WP_CLI::halt(1)` on failure, groups dispatched by name via `--only=<group>`
- New shared helpers load via a `// Shared helpers` sub-block in `hl-core.php` above the `// Email system` block (sits at line 131-133 as of Task 1)

## Helpers already available

- `HL_Audit_Service::log( $action_type, $data = array() )` — now never throws, never blocks caller. Internally: try/catch wrap + `$wpdb->insert === false` check both route to `self::record_audit_failure()` which error_logs `[HL_AUDIT_FAIL]` + bumps daily counter `hl_audit_fail_count_YYYY-MM-DD` (autoload=no). The counter bump is itself wrapped in try/catch for hard-DB-outage safety.
- `HL_Audit_Service::get_last_event( $entity_id, $action_type ): ?array` — returns the most recent `hl_audit_log` row (as ARRAY_A with `actor_name` JOIN) matching entity + action, or `null`. Uses `ORDER BY created_at DESC LIMIT 1`. **Performance note:** no composite index on `(entity_id, action_type, created_at)` today — Task 6 schema revision should add `KEY entity_action_time` if Force Resend history becomes a hot path. Single-col `entity_id` index keeps today's usage fine (low row counts). **Caller note:** only works when the audit row has a non-null `entity_id` — the `email_token_alias_hit` audit doesn't pass one, so tests for it must query `hl_audit_log` directly by `action_type`.
- `HL_Email_Recipient_Resolver` now supports 3 new token behaviors (Track 3 Task 23):
  - `assigned_mentor` — new token. Resolves via `hl_team_membership` (`membership_type='mentor'`), scoped to cycle via `hl_team.cycle_id`. Requires `user_id` + `cycle_id` in context; audit-logs `email_resolver_missing_context` on missing context. Excludes self-mentor case (triggering user who is themselves their team's mentor) with `mentor_tm.enrollment_id <> user_tm.enrollment_id`.
  - `observed_teacher` — renamed from `cc_teacher`. Reads BOTH `observed_teacher_user_id` (new) and `cc_teacher_user_id` (legacy) context keys so existing callers keep working.
  - `cc_teacher` — legacy alias, routes to `resolve_observed_teacher()` + emits `email_token_alias_hit` audit on every call (A.6.11 deprecation telemetry, removal target: 90 days of zero hits). Only current caller is `class-hl-email-automation-service.php:413` for classroom visit emails — low volume, no flood risk near-term.
  - The resolver class docblock token list now reads `triggering_user, assigned_coach, assigned_mentor, school_director, observed_teacher (alias: cc_teacher), role:X, static:email`.
- `HL_Email_Condition_Evaluator` (`includes/services/class-hl-email-condition-evaluator.php`) — now routes `enrollment.roles` conditions through `HL_Roles`. The branch is added via an early-return `if` block just above the generic `switch ( $op )` inside `evaluate_single()`. If you need to route another polymorphic field (e.g. `team.roles`), follow the same pattern: guard on `$field === '<name>' && class_exists('HL_Roles')`, handle each op, early-return per case. Unsupported ops (`gt`/`lt`) return `false` explicitly.
- `HL_Roles` (`includes/services/class-hl-roles.php`) — static-only, 4 methods:
  - `HL_Roles::parse_stored( mixed $stored ): array` — format-agnostic reader. Accepts JSON array, CSV, array, or null/empty. JSON objects (`{...}`) rejected → `[]`. Non-string entries filtered. Lowercased + trimmed + deduped. Order of returned slugs is not guaranteed.
  - `HL_Roles::has_role( mixed $stored, string $role ): bool` — exact-match role check. Fixes the `LIKE '%leader%'` substring bug.
  - `HL_Roles::sanitize_roles( $roles ): string` — normalise to canonical CSV (sorted alphabetically). Drops entries containing `,`. Returns `''` on empty/invalid.
  - `HL_Roles::scrub_is_complete(): bool` — reads `HL_Roles::OPTION_SCRUB_DONE` (`'hl_roles_scrub_done'`). Callers gate `FIND_IN_SET` usage on this.
  - Constant: `HL_Roles::OPTION_SCRUB_DONE = 'hl_roles_scrub_done'`
- `HL_CLI_Email_V2_Test` (`includes/cli/class-hl-cli-email-v2-test.php`) — CLI test harness registered as `wp hl-core email-v2-test`. Groups: `roles` (filled), `schema`/`cron`/`drafts`/`resolver`/`deliverability`/`audit` (stubs — filled by later tasks). Support `--only=<group>` flag.

## Known deviations from plans

- **Task 1** placed the `HL_Roles` require in a new `// Shared helpers` sub-block above the `// Email system` block in `hl-core.php`, rather than literally "immediately before `class-hl-email-recipient-resolver.php`" as the plan text said. Phase B and Phase D reviewers both accepted this as a reasonable grouping for a generic (non-email) helper.
- **Task 1** added type hints (PHP 7.4 scalar + return type) and a class constant (`OPTION_SCRUB_DONE`) beyond the literal plan text, per Phase D Sr SWE feedback. The plan's verbatim PHP had no type hints and used a magic option string.
- **Task 1** added a `{` rejection branch to `parse_stored()` so JSON objects yield `[]` instead of being tokenised as CSV garbage. This is new behaviour not specified in the plan but matches the docblock contract and closes a latent quirk Phase D flagged.

---

## Task log

### 2026-04-11 — Track 3 Task 1: HL_Roles helper class
- Created `includes/services/class-hl-roles.php` (~117 lines): 4 static methods, 1 class constant, PHP 7.4 type hints, JSON + CSV format-agnostic parser with `{...}` rejection.
- Created `includes/cli/class-hl-cli-email-v2-test.php` (~155 lines): CLI harness + filled `test_roles()` with 12 assertions covering `has_role` both formats both polarities, `sanitize_roles` happy + poison, `parse_stored` null/empty/object/mixed-type edges, `scrub_is_complete` both states with option cleanup.
- Modified `hl-core.php`: added `// Shared helpers` sub-block + require of `class-hl-roles.php` (line 131-133), added require + register for the CLI test class.
- Quality gate passed: Phase B (2 reviewers PASS), Phase D (Sr SWE 7.2/10 PASS, WP Expert 9.4/10 PASS). Phase E applied 4 hardening fixes inline (type hints, const, docblock fix, JSON object rejection, expanded tests). Phase G: 0 user-visible error risk (no callers yet — Task 2 will be the first consumer).
- **NOT YET DEPLOYED.** Syntax will be verified on test server at the 4-task foundation checkpoint (after Tasks 1, 2, 5, 23).
- Commit: `c1b6e86`

### 2026-04-11 — Track 3 Task 2: Route condition evaluator through HL_Roles
- Modified `includes/services/class-hl-email-condition-evaluator.php` (+37 lines): inserted a role-aware branch in `evaluate_single()` between `resolve_field()` and the generic `switch ( $op )`. Branch activates on `$field === 'enrollment.roles'`, routes `eq`/`neq`/`in`/`not_in`/`is_null`/`not_null` through `HL_Roles::has_role()` / `parse_stored()`, rejects unsupported ops (`gt`/`lt`) with explicit `return false`.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+93 lines): replaced `test_resolver()` stub with 13 assertions covering JSON + CSV happy path, false-match regression for both formats, all 6 op boundaries, case-insensitive matching, and unsupported-op rejection.
- Quality gate: Phase B combined reviewer PASS, Phase D Sr SWE 8.5/10 PASS, WP Expert 9.4/10 PASS. Phase E added docblock note on `is_null`/`not_null` semantic divergence + 6 boundary assertions (`is_null` on PHP null, `neq` positive, `not_null` positive, unsupported op rejection, `in` zero-match, `neq` negative symmetry, case-insensitive match). Phase G: 0 user-visible error risk (workflows using valid role values — `teacher`, `mentor`, `coach`, `school_leader` — don't collide under strict matching).
- Deferred design concerns (NOT applied in Task 2, belong to a future refactor): hard-coded `'enrollment.roles'` string literal could be extensibility-scoped via `HL_Roles::is_role_field()` registry, `class_exists('HL_Roles')` guard is defensive-but-smelly (could be hard-required), request-scoped memoization on `parse_stored` for high-throughput evaluation paths. All flagged by Phase D Sr SWE; all out of scope for Task 2's plan text.
- **First consumer of `HL_Roles`** — proves the helper works via the CLI test flow. Still not deployed; syntax verified at the 4-task foundation checkpoint.
- Commit: `41d8907`

### 2026-04-11 — Track 3 Task 5: HL_Audit_Service::get_last_event + hardened log()
- Modified `includes/services/class-hl-audit-service.php` (+62/-16): wrapped `log()` body in `try { ... } catch ( \Throwable $e )`, added `$wpdb->insert === false` check, factored the failure path into a new private `record_audit_failure()` helper that itself catches any Throwable. Added public `get_last_event( $entity_id, $action_type ): ?array` with `LEFT JOIN` on `{$wpdb->users}` for `actor_name`. Fixed a pre-existing `cycle_id` column-alignment typo.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+39/-1): filled `test_audit()` with 3 assertions + `$wpdb->delete` cleanup of the test rows so we don't pollute the audit log.
- Quality gate: combined Phase B+D reviewer PASS. One real hole caught: `$wpdb->insert` returns `false` on SQL errors WITHOUT throwing, so the plan's verbatim `try/catch` alone would miss the most common failure mode. Fixed inline by adding the return-value check + extracting a shared `record_audit_failure` helper. Schema concern noted: `hl_audit_log` has single-col indexes on `entity_id` and `action_type` but no composite `(entity_id, action_type, created_at)` — fine for today's low row counts, flag for Task 6 schema revision if Force Resend history becomes hot. Phase G: 0 user-visible error risk (all changes are infrastructure hardening; happy-path behavior identical to pre-Task-5).
- Track 1 Task 14 (Force Resend history display) now has its required helper.
- Commit: `8cbaa3a`

### 2026-04-11 — Track 3 Task 23: assigned_mentor resolver + cc_teacher alias
- Modified `includes/services/class-hl-email-recipient-resolver.php` (+102/-5): added `assigned_mentor` token case, renamed `resolve_cc_teacher()` → `resolve_observed_teacher()` with dual context key support, added new `resolve_assigned_mentor()` with 3-step SQL (enrollment → team mentor → user). Kept `cc_teacher` as a legacy alias that emits `email_token_alias_hit` audit on every hit. Updated class docblock token list.
- Modified `includes/cli/class-hl-cli-email-v2-test.php` (+65): appended `test_resolver` smoke test for `assigned_mentor` + `cc_teacher` alias audit verification. Uses `log_id` snapshot pattern for bounded cleanup (never wipes pre-existing deprecation telemetry) and avoids the `gmdate` vs server-TZ `CURRENT_TIMESTAMP` drift issue the combined reviewer caught.
- Quality gate: combined reviewer caught one BLOCKING bug (self-mentor edge case — a user who is themselves the team mentor would be returned as their own mentor via the self-join). Fixed inline by adding `AND mentor_tm.enrollment_id <> user_tm.enrollment_id` to the join. Two SHOULD-fix test hygiene issues (destructive cleanup, TZ-mismatched query window) also fixed inline via the log_id snapshot pattern. Weak smoke assertion (`is_array` passes on empty result) accepted as-is — strengthening it would require synthetic fixture data. Phase G: 0-1/10 user-visible error risk — `assigned_mentor` is a NEW token with no existing callers; `cc_teacher` alias's dual-key reading preserves existing caller semantics.
- Schema confirmations made during review: `hl_team_membership` has `(team_id, enrollment_id, membership_type ENUM('mentor','member'))` matching the plan's assumption. `hl_enrollment.status` enum is `('active','inactive')` — the `'warning'` literal is dead but harmless (kept for parity with existing cron queries per Pre-Flight note 6).
- **ALL FOUR TRACK 3 FOUNDATION PREREQUISITES (Tasks 1, 2, 5, 23) now committed.** Next step: deploy the branch to the test server and run `wp hl-core email-v2-test` to verify all four CLI test groups pass. If green, merge `feature/email-v2-track3-backend` to `main` and start Track 1 (which depends on these four). Remaining Track 3 tasks (3, 4, 6-22, 24-32) can run in parallel with Track 1 after the merge.
- Commit: `3190f63`
- **Post-foundation deploy checkpoint** (2026-04-11): branch deployed to test server via SCP tarball. `wp hl-core email-v2-test --only=roles` 12/12 PASS, `--only=resolver` 16/16 PASS, `--only=audit` 3/3 PASS. `wp hl-core smoke-test` showed 6 pre-existing `hl_user_profile/completion_pct` failures (all unrelated to Track 3 changes — `completion_pct` lives in frontend files this branch never touched). Merged to main as `2b120d7` via `--no-ff`. Pushed origin/main.

### 2026-04-11 — Track 3 Foundation Polish pass
Cold-read fresh-eye audit found zero blockers but 10 polish items worth fixing before adding more Track 3 work or starting Track 1. Executed in order per the polish brief. Re-merge to main required — polish commits are on top of `2b120d7`.

- **§1 Tasks 3+4 — gated FIND_IN_SET migration** (resolver). `resolve_school_director()` and `resolve_role()` now use `scrub_done()` gate: post-Rev-37 path = FIND_IN_SET; pre-scrub = LIKE narrow + `HL_Roles::has_role()` PHP post-filter. Unified post-filter runs in both branches as defense-in-depth. Comma-poison rejection added to `resolve_role()` with `email_resolver_rejected_role` audit trail. New private `HL_Email_Recipient_Resolver::scrub_done()` helper — single removal point for Task 32's `class_exists` cleanup. CLI test_resolver gains substring-regression assertion (skips if fixture data lacks a clean school_leader cycle) + poison rejection assertion with bounded log_id cleanup. **Full quality gate run** (Phase B: 2 parallel reviewers, Phase D: Sr SWE + WP expert). 1 must-fix from Phase B#2 (poison test audit cleanup), resolved inline via log_id snapshot. 1 helper extraction from Phase D Sr SWE, resolved inline. Phase D WP expert surfaced a spurious schema concern (FIND_IN_SET vs JSON) — countered by the scrub_is_complete() gate + unified post-filter, not a Task 3/4 issue. Commit: `f2359e5`.
- **§3 Atomic audit failure counter** (audit service). `record_audit_failure()` now uses `UPDATE wp_options SET option_value = CAST(option_value AS UNSIGNED) + 1 WHERE option_name = %s` followed by `add_option(...)` on zero-affected-rows. Closes a read-modify-write race where concurrent failure cascades would lose increments via the prior `get_option` + `update_option` pattern. `add_option` is atomic via the option_name UNIQUE key — two racers both seeing a missing row produce one insert + one ignored duplicate-key error, not a lost increment. Outer try/catch preserved.
- **§4 Rate-limited missing-context audit** (resolver). `resolve_assigned_mentor()` gates its `email_resolver_missing_context` audit call on a 5-minute transient `hl_resolver_missing_ctx_assigned_mentor`. A misconfigured cron workflow firing every few minutes now writes one row per 5 minutes instead of N rows per tick.
- **§5 Typo guard on evaluator** (condition evaluator). Added a `preg_match('/^enrollment\.roles?$/i', $field)` branch after the role-aware branch. Catches `enrollment.role` (singular), `Enrollment.Roles` (case), etc. Fail-closed (returns `false` — workflow doesn't silently match). Emits `email_condition_field_typo` audit row with per-field-sha1 transient rate limit (`hl_condition_typo_<sha1>`). CLI assertion in test_resolver proves `enrollment.Roles` fails closed and emits the audit row, with bounded log_id cleanup + transient cleanup.
- **§6 Strengthened assigned_mentor smoke test**. Rewrote to select a fixture where a team has BOTH a member AND a distinct mentor (self-join excludes self-mentor). Asserts `count === 1` AND the returned user_id matches the team's mentor enrollment's user_id (queried explicitly). Added second self-mentor exclusion assertion: find a lone-mentor team, resolve `assigned_mentor` with that mentor as context, assert empty result. Both assertions [SKIP] via `WP_CLI::log` when fixture data is unavailable — never fake passes.
- **§7 get_last_event zero-entity guard** (audit service). Early return `null` when `$entity_id <= 0`. Without this, `get_last_event(0, ...)` matched every audit row with a null or zero entity_id (including system-level events that never passed one). CLI assertion added.
- **§8 Stale token list fixes**. `README.md:75` and `class-hl-admin-emails.php:287` both listed `cc_teacher` as a canonical token and omitted `assigned_mentor`/`observed_teacher`. Both updated to: `triggering_user, assigned_coach, assigned_mentor, school_director, observed_teacher, role:X, static:email (legacy alias: cc_teacher)`.
- **§9 Deferrals routed to correct downstream sessions.**
  - Track 3 plan Task 6 header now documents adding composite index `KEY entity_action_time (entity_id, action_type, created_at)` on `hl_audit_log` during Rev 35 migration with idempotency guard.
  - Track 3 plan Task 32 now has explicit polish-era cleanup sub-items: drop `class_exists('HL_Roles')` / `class_exists('HL_Audit_Service')` defensive guards (single removal point via `HL_Email_Recipient_Resolver::scrub_done()`) + verify the 2026-07-10 cc_teacher reminder is still present in STATUS.md.
  - Track 1 plan already lists `assigned_mentor`, `observed_teacher` canonical, and `cc_teacher` alias-only acceptance in the token registry + recipient picker task (verified via grep on lines 144-146, 424, 440, 537-538). No additions required.
  - STATUS.md gains a new `## Scheduled reviews` section with a dated 2026-07-10 entry for the cc_teacher 90-day deprecation review.
  - New memory `project_cc_teacher_deprecation_2026_07.md` saved with the same reminder + removal steps + spec ref (A.6.11).
- **§9 items NOT applied inline** (per brief — these are scope-creep):
  - `rejected_role` audit rate limit — §4 only mentioned `missing_context`. Flagged in reviewer synthesis but left untouched.
  - DB error swallowing (`$wpdb->get_results` returning null treated as empty) — flagged by Sr SWE, not in brief.
  - Seeded-fixture for Task 3 regression test — the brief explicitly said "if no school_leader row exists, log [SKIP]".
- Polish commits: `f2359e5` (Tasks 3+4 gated migration) + `6cc01dd` (polish bundle §3-§9).
- **Post-polish deploy checkpoint** (2026-04-11): all polish commits deployed to test server via SCP tarball. `wp hl-core email-v2-test` reports **39 passed, 0 failed** across all groups (roles 12, resolver 23, audit 4 — up from 31 pre-polish). `--only=resolver` and `--only=audit` dispatch separately confirmed working. `wp hl-core smoke-test` shows the same 6 pre-existing `hl_user_profile/completion_pct` failures (unchanged from the pre-polish baseline — polish pass introduced zero regressions).
- Critical wins on the live server: the strengthened `assigned_mentor` smoke test (§6) fired without [SKIP] — test server seed data contains a team with both a member and a distinct mentor, so the `count===1` assertion ran with `expected 1, got 1` and the `user_id` match landed with `expected 1571, got 1571`. The self-mentor exclusion test also ran (not skipped), confirming `expected 0, got 0`. The typo guard (§5) fired: `enrollment.Roles (capital R typo) fails closed` AND `emits email_condition_field_typo audit row` — both on real audit writes with bounded log_id cleanup. Every assertion is LIVE-DB exercised, not fake-passing via [SKIP] shortcuts.
- **Next step (post round-1): Session A (Track 3 continuation, Tasks 6-22, 24-32) and Session B (Track 1 admin UX) cleared to dispatch. Session C (Track 2 builder) clear anytime.** Track 3 foundation is now complete with polish — no further foundation work blocking downstream sessions.

### 2026-04-11 — Track 3 Foundation Polish pass — ROUND 2 (sealed)
Two items flagged as out-of-scope during round 1, executed as a sealed polish round before any Session A/B/C work starts.

- **R2 §1 — Rate-limit `email_resolver_rejected_role` audit.** Applied the same per-value transient pattern used by round-1 §5 typo guard (but with a 16-char sha1 prefix for key length consistency — minor nit noted below). Key format: `hl_resolver_rejected_role_` + `substr(sha1($normalised_role), 0, 16)`. TTL `5 * MINUTE_IN_SECONDS`. Write-once via `get_transient` / `set_transient`. The `return array()` fail-closed control flow is unchanged — only the audit write is gated. A misconfigured workflow retrying the same poison value every cron tick now writes at most 1 row per 5 minutes instead of N per tick.
- **R2 §2 — DB error observability.** New private `HL_Email_Recipient_Resolver::log_db_error_if_any( string $method_label ): void` helper. Reads `$wpdb->last_error`, snapshots the first 200 chars, RESETS `$wpdb->last_error` to `''` unconditionally (load-bearing for downstream query attribution), then rate-limits via per-method sha1 transient (`hl_resolver_db_error_` + 16-char sha1). Emits `email_resolver_db_error` audit row with `reason => label . ': ' . snippet` and `entity_type => 'email_workflow'`. The helper lives in the `// Helpers` section of the class, right after `scrub_done()`. Added 9 physical call sites across 4 resolver methods:
  - `resolve_assigned_coach()` — 1 site, label `assigned_coach:lookup`
  - `resolve_school_director()` — 3 sites: `school_director:school_lookup` (after school_id query) + `school_director:director_lookup` (both post-scrub FIND_IN_SET branch AND pre-scrub LIKE branch — shared label since they're the same query path semantically)
  - `resolve_assigned_mentor()` — 3 sites: `assigned_mentor:enrollment_lookup`, `assigned_mentor:mentor_join`, `assigned_mentor:user_lookup`
  - `resolve_role()` — 2 sites: `role:user_query` in both scrub branches
  - Post-filter `foreach` loops and PHP-side typo/poison rejection branches are NOT instrumented (not DB calls).
  - Fail-closed contract preserved: DB error still returns `array()`, the helper only observes and audits.
- **Helper order invariant (verified by both Phase B and Phase D reviewers):** `snapshot last_error → reset last_error = '' → (class_exists + transient guards) → audit write → set_transient`. The reset runs BEFORE the audit write so even if `HL_Audit_Service` is missing or the rate limit fires, `$wpdb->last_error` is cleared.
- **CLI test coverage added to `test_resolver()`** (3 new assertion blocks):
  - **R2 §1 rate-limit test**: uses a distinct poison value (`teacher,mentor,coach`) from the existing round-1 poison test (`teacher,mentor`) so transient keys don't collide. Clears transient, snapshots `log_id`, calls `resolve()` twice back-to-back with the same poison, asserts exactly 1 new row (not 2). Cleans up via bounded `DELETE ... WHERE log_id > snapshot` and clears the transient.
  - **R2 §2 happy-path negative**: added INSIDE the `$mentor_fixture` branch right after the existing `user_id` match assertion. Runs a fresh `assigned_mentor` resolve on the known-good fixture and asserts 0 new `email_resolver_db_error` rows emit above snapshot. Skips cleanly with the rest of the mentor fixture block if no fixture data is available.
  - **R2 §2 forced-error positive**: uses `\Closure::bind` to invoke the private helper directly. Injects `$wpdb->last_error = 'SIMULATED_TEST_ERROR_DO_NOT_ALARM'`, binds a closure to `HL_Email_Recipient_Resolver::instance()` with class scope, invokes `log_db_error_if_any('role:user_query')`, then asserts (a) exactly 1 new `email_resolver_db_error` row with reason matching `%SIMULATED_TEST_ERROR_DO_NOT_ALARM%` AND (b) `$wpdb->last_error === ''` after the helper call. Cleans up bounded row + transient. The `Closure::bind` approach was explicitly authorized by the brief as the cleanest test technique (alternatives: test-only public wrapper is invasive, reflection is equivalent coupling with uglier syntax).
- **Quality gate**: Phase B combined reviewer (1 agent, both SQL/WP lenses) PASS with 1 non-blocking nit (25-line deliberation comment block in Assertion C). Phase D combined reviewer (Sr SWE 9/10 + WP Expert 10/10) PASS with the same nit + a sha1 prefix-length consistency nit (round-1 typo guard uses full `sha1($field)` vs round-2 uses `substr(sha1, 0, 16)`). Phase E: trimmed the dead commentary block to 6 lines inline. sha1 prefix consistency nit NOT addressed — changing round-1's key would orphan existing transients and is out-of-brief scope. **Phase G: 0/10 user-visible error risk** — this round adds pure observability (no return-value changes, no SQL changes, fail-closed preserved) and reuses the round-1 typo-guard rate-limit pattern which is already production-verified.
- **Items explicitly NOT applied** (per brief "sealed round" scope): no other resolver methods touched, no evaluator changes, no audit service changes, no plan file changes, no STATUS/README changes (foundation checkboxes already marked, no new feature surface).
- **Expected assertion count delta after R2**: round-1 finished at 39 (roles 12 / resolver 23 / audit 4). R2 adds 4 resolver assertions (1 rate-limit + 1 happy-path negative + 2 forced-error). Expected post-R2 total: **43** on a test server with the mentor fixture present (if mentor fixture absent, the happy-path negative skips and total is 42).
- Commit: `8413ebb`
- **Post-commit deploy checkpoint** (2026-04-11): deployed via SCP tarball. `wp hl-core email-v2-test --only=resolver` reported 27/27 PASS including all 4 new R2 assertions (rate-limit, happy-path zero errors, forced-error with SIMULATED marker, last_error reset). The full sweep (`wp hl-core email-v2-test` without --only) initially reported **42 passed / 1 failed** — the round-1 existing poison rejection test failed because my new R2 rate limit blocked its audit write on the second run within a 5-minute window. Root cause: round-1's poison test (`teacher,mentor`) didn't clear the rate-limit transient that R2 §1 now sets. Fix in commit `<below>`: added `delete_transient($poison_lock)` before AND after the round-1 poison test so it's self-contained and idempotent across runs. Re-deployed, re-ran: full sweep **43/43 PASS**, `--only=resolver` 27/27, `--only=audit` 4/4, `--only=roles` 12/12. Smoke test: same 6 pre-existing `hl_user_profile/completion_pct` failures — no new regressions. Commit: `<pending — cross-test hygiene fix>`
